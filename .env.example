# GitHub Models (https://models.github.ai/inference) — free/prototyping-only,
# rate-limited. Swap these three values for an org's own OpenAI-compatible
# endpoint in production; no code change is required.

# A GitHub personal access token with the "models" permission.
GITHUB_MODELS_TOKEN=ghp_your_token_here

GITHUB_MODELS_BASE_URL=https://models.github.ai/inference
GITHUB_MODELS_MODEL=openai/gpt-4o-mini

# Max Flesch-Kincaid grade level the draft may score. Issue #1 originally set
# this at 6 (NIH/AMA/HHS patient-materials guidance); raised to 8 after live
# testing showed the model's non-red-flag prose reliably ran 7-8 on realistic
# notes. See service/verifier/reading_level.py for the exemption rule this
# doesn't affect (red-flag content is never gated by this at all).
READING_LEVEL_MAX_GRADE=8
